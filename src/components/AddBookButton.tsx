"use client";

export default function AddBookButton() {
  return (
    <div className="flex justify-center">
      <button
        className="py-2 px-4 rounded-lg hover:bg-white border-gray-400 border-2 hover:text-black"
        onClick={() => {}}
      >
        Add Book
      </button>
    </div>
  );
}
